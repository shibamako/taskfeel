<%= form_with(model: status) do |form| %>
  <% if status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(status.errors.count, "error") %> prohibited this status from being saved:</h2>

      <ul>
        <% status.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :status_item %>
    <%= form.text_field :status_item %>
  </div>

  <div class="field">
    <%= form.label :serialize %>
    <%= form.number_field :serialize %>
  </div>

  <div class="field">
    <%= form.label :mukou %>
    <%= form.check_box :mukou %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
